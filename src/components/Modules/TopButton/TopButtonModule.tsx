import { UpButtonAtom } from "~/components/Atoms/UpButtonAtom/UpButtonAtom";

type Props = {
  // Add props here
};

export const TopButtonModule = (props: Props) => {
  return (
    <div>
      <UpButtonAtom />
    </div>
  );
};
